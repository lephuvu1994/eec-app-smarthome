export type TSignupVariables = {
  identifier: string;
  password: string;
  firstName?: string;
  lastName?: string;
};

// ============================================================
// API SERVICE
// ============================================================

