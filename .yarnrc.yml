# Yarn 4 (Berry) - dùng node_modules thay vì PnP (tương thích Expo/React Native)
nodeLinker: node-modules

# Tắt interactive prompt trên CI/EAS
preferInteractive: false

# Disable global cache trên CI để tránh permission issue
enableGlobalCache: false
